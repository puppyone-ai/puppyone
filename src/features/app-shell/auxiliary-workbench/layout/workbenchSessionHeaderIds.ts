export function workbenchTabId(sessionId: string) {
  return `desktop-terminal-tab-${sessionId}`;
}

export function workbenchPanelId(sessionId: string) {
  return `desktop-terminal-panel-${sessionId}`;
}
