/** Slightly above the CSS total (≈1.1s) so a missed animationend never strands the user. */
export const EMPTY_STATE_INTRO_FALLBACK_TIMEOUT_MS = 1_400;
export const EMPTY_STATE_INTRO_REDUCED_MOTION_DURATION_MS = 220;
