# 文件解析配置
# 解析方式：'mineru' 使用 MinerU API，'local' 使用本地 Python 库
# 推荐：本地开发使用 'local'，生产环境根据需求选择
FILE_PARSER_MODE=local

# MinerU API Config（仅在 FILE_PARSER_MODE=mineru 时需要）
# 注意：MINERU_API_URL 应该是基础 URL，不要包含 /api/v4
MINERU_API_URL=https://mineru.net
MINERU_API_TOKEN=your-api-token-here
MINERU_TIMEOUT=300
MINERU_OUTPUT_FORMAT=markdown
# MinerU 文件访问 URL（用于生成 MinerU 可访问的文件 URL）
# 如果为空，则使用 NEXT_PUBLIC_API_URL
# 重要：这个 URL 必须能被 MinerU 服务器访问（不能是 localhost）
# 本地开发可以使用 ngrok: https://your-domain.ngrok.io
# 生产环境使用实际域名: https://your-domain.com
MINERU_FILE_BASE_URL=