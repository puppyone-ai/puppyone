// A login token is not an inference credential. Enable only with a server-owned gateway contract.
export const managedComputeAvailability = Object.freeze({ available: false, reason: "gateway-unavailable" });
