/**
 * Shared layout widths for project detail surfaces.
 *
 * 900px is the default "content rail" for project pages that are neither
 * raw data grids nor monitoring canvases: markdown docs, settings forms,
 * access-point detail, and commit detail all fit this width without becoming
 * sparse on wide displays.
 */
export const PROJECT_CONTENT_RAIL_WIDTH = 900;
