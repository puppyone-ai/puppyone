# puppyone をはじめる

あなたと Agent のための、ローカルファイル・ワークスペースです。

このプロジェクトは、コンピューター上のフォルダーです。このガイドも通常の Markdown ファイルなので、編集、名前の変更、移動、削除ができます。削除しても自動的には再作成されません。

## 最初の編集をする

下の行を、取り組みたい内容に書き換えてください。macOS では ⌘S、Windows と Linux では Ctrl+S で保存できます。

最初のプロジェクト：…

## ファイルを追加する

ファイルエクスプローラーへファイルをドラッグするか、+ メニューからドキュメントを作成します。Markdown、表、その他のファイルはこのプロジェクトフォルダーに保存され、ほかのアプリからも引き続き利用できます。

既存のフォルダーや Obsidian Vault がある場合は、そのフォルダーをそのままプロジェクトとして開けます。Notion は Markdown と CSV でエクスポートして解凍し、Airtable は各テーブルを CSV でエクスポートしてください。既存フォルダーを開いた場合、このガイドは追加されません。

## Agent と作業する

準備ができたら、Agent パネルまたは設定から Agent を選び、構成します。ノートの整理やファイルの作業を依頼できます。提案された変更は、反映する前に確認してください。

例：「最初のプロジェクトのアイデアを短い計画にまとめてください。」

プロジェクトを作成しただけでは、Agent の起動、コマンドの実行、ファイルのアップロードは行われません。クラウドモデルを選んだ場合、Agent に送信した内容はそのモデルの提供元によって処理されることがあります。ローカル保存は、Agent との会話が常にこのコンピューター内だけに留まることを意味しません。

## 自分のワークスペースにする

役に立つ間はこのガイドを残しておき、いつでも自分のノートに置き換えてください。`README.md` はプロジェクト自身の説明に利用できます。
