/**
 * Numeric mirror of the CSS geometry contract for canvas, SVG, and virtual
 * layout code that cannot consume a CSS custom property directly.
 * Architecture tests keep this value aligned with control-geometry.css.
 */
export const STANDARD_CONTROL_SIZE = 32;
