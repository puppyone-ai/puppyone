"""Chunking module (core).

This package provides:
- JSON traversal to extract large string nodes (RFC6901 JSON Pointer paths)
- Character-based chunking with overlap
- Idempotent persistence entrypoints (repository-backed)
"""
