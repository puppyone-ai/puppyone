"""Vendored copy of the scope-sync sidecar, bundled so it ships with the
backend package (the canonical source is sandbox/scope-sync-sidecar/, outside
backend/, and is NOT in the deployed image). Kept byte-identical by
test_sidecar_bundle_matches_canonical. Do not edit here — edit the canonical."""
