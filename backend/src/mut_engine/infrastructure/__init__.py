"""Storage and low-level Git-format utilities for the version engine.

This package is the PuppyOne-owned home of code that previously lived in
the ``mut`` Python package's ``foundation/`` and ``core/`` modules. With
the MUT wire protocol removed, these are no longer a separate runtime
dependency — they are now first-class PuppyOne code.
"""
