# Puppyone 产品与收费逻辑

> 状态：已确定的产品决策，实施进度见第 7 节。
> 更新日期：2026-09-21。
> 负责人：Product；Desktop、Cloud 和 PuppyPay 按本文实施。
> 本文是当前收费逻辑的统一依据。历史文档中冲突的套餐价格、AI 捆绑和云端 Runtime 收费方案，以本文为准。

## 1. 我们到底按什么收费

**云托管按 $15 / 人 / 月订阅；内置 AI 的算力单独充值，按实际使用扣除 AI token 余额。两项独立购买。**

| 用户购买的东西 | 怎么付钱 | 买到什么 |
| --- | --- | --- |
| Pro 云托管 | 每个付费席位 $15 / 月，按月续订 | 云托管服务及对应席位权益 |
| AI token 余额 | 一次性充值，用户可以随时再次充值 | 使用 Puppyone 提供的模型算力，按用量扣余额 |

用户可以只买云托管，也可以只充值 AI，还可以两者都买。**使用 AI 不要求先购买 Pro，也不要求把项目上传到云端。**

## 2. $15 的 Pro 订阅包含什么

$15 是每个付费席位每月的云托管费。一个付费席位是 $15 / 月，三个付费席位是 $45 / 月。团队托管订阅由所属组织统一管理。

- Pro 不包含、也不赠送 AI token 余额。只想买云托管的人不必为 AI 预付钱。
- Pro 不提供云端 Agent 或云端 Runtime；当前 Agent 在用户本机运行。
- AI 余额不能用来抵扣 Pro 订阅费；订阅费也不会转成 AI 余额。
- 取消 Pro 不清除个人 AI 余额，也不影响用户继续在本地使用已充值的 AI 算力。
- 存储容量、角色对应的付费席位范围、席位增减和取消的生效规则，需要在托管商品上线前另行明确；本文不承诺无限存储。

## 3. AI token 余额怎么充值、怎么扣

对用户统一称为 **AI token 余额**，以美元显示。这个名字表示用于 AI 的消费余额，不表示预先买到了固定数量的 token；不同模型的 token 单价不同。

### 充值

1. 用户先通过邮箱登录 Puppyone。
2. 用户选择充值金额，完成一次性支付。
3. 支付成功经服务端确认后，余额增加。充值 $100，就增加 $100 的可消费余额；适用税费在付款前单独展示。
4. 余额保存在用户的 Puppyone 个人账户中，由 PuppyPay 的数据库和账本记录，不只存在本机。

用户可以按需多充、少充，随时继续充值。具体支持的档位和单笔上下限由服务端商品目录提供，支付前清楚展示。

**AI 不订阅、不自动续费、不自动充值，余额不按月清零。** 用户没有使用，就不发生模型消费；用完后自行充值。个人余额不会因切换组织而变成组织共享余额。

### 消费

使用 Puppyone 提供的模型算力时，按所选模型的对客价格和实际用量扣余额。模型选择处应能查看价格；输入、输出，以及适用的缓存、推理等计费项目要说明清楚。

一次 Agent 任务可能调用模型多次，每次调用的费用累计为该任务的 AI 消费。历史上下文、工具结果再次发送给模型时，也可能产生输入用量。本地工具执行和等待时间本身不按时长收费。

例如：用户充值 $100，累计产生 $40 的 AI 消费，账户还剩 $60。这里的 $40 已包含我们的服务收益，不再另外扣一笔“30% 服务费”。

调用开始前可以暂时预占一部分余额，完成后按实际用量结算并释放未使用部分。页面应区分可用余额、暂时冻结金额和已消费金额。可用余额不足以启动下一次付费调用时，提示用户充值；不自动从银行卡扣款。

用户使用自己的模型账号、API Key 或本地模型时，不扣 Puppyone 的 AI token 余额；第三方服务的费用由用户与其提供商结算。

## 4. 我们赚多少：AI 业务真实毛利率 30%

**AI 算力业务的定价目标是真实毛利率 30%。** 这一目标针对 AI 消费收入；Pro 云托管的成本和毛利单独核算。

计算口径：

```text
AI 毛利率 =（AI 消费收入 − 对应直接成本）÷ AI 消费收入
目标毛利率 = 30%
目标 AI 售价 = 对应直接成本 ÷ 0.70
```

“对应直接成本”包括模型供应商实际费用、支付渠道费用、供应商充值手续费，以及可归属于这部分 AI 服务的网关等直接运行成本。相关费用按统一口径分摊到消费中，不能漏掉或重复计算。代收税费不算我们的收入；毛利也不等于扣除研发、市场等全部经营开支后的净利润。

例如，一笔 AI 消费给我们带来 $1.00 收入，其中对应直接成本合计 $0.70，则毛利为 $0.30，毛利率为 30%。如果直接成本为 $1.00，则售价应约为 $1.4286。

**不能把“成本乘以 1.30”写成 30% 毛利。** 成本 $1.00、售价 $1.30 时，毛利率只有约 23.08%。同样，仅把模型供应商成本除以 0.70，在扣除支付等其他直接成本后，也达不到真实 30% 毛利。

实施时要根据完整成本确定对客模型价格，并持续核对实际毛利。若渠道费用与售价成比例，要把这部分一并纳入定价计算。30% 是定价与运营核算目标，不是声称每笔交易在手续费、退款和取整后都恰好达到 30%。

用户充值时不扣掉 30%：充值 $100，余额仍增加 $100。收益通过后续模型消费价格实现；不能把尚未消费的充值款直接当成已经赚到的毛利。

模型售价必须在使用前明确，账本分别记录供应商成本、用户实际扣款和对应价格版本。价格调整适用于后续调用，不事后追改已完成调用的收费。

## 5. 怎么呈现给用户

### 定价 / Pro 订阅页

只展示云托管订阅及其权益。主文案：

> **Pro · $15 / 人 / 月**
>
> 为你的项目购买云托管。AI 用量单独充值，按需使用。

这里不放 AI 充值套餐卡，不把 AI 余额作为会员等级，也不把购买 Pro 设为充值 AI 的前提。

### 个人账户 → AI token 余额

这是独立的余额页面，展示可用余额、充值入口、充值记录和消费明细。主文案：

> **AI token 余额**
>
> 充值后即可使用内置 AI。充多少，用多少。
>
> 余额以美元计价，按所选模型的价格和实际用量扣除。

按钮使用“充值 $X”，不使用“订阅 AI”或“升级 Pro”。付款前展示付款金额、到账余额和适用税费。30% 毛利是内部定价规则，用户看到的是明确的最终模型价格。

### 右侧内置 Agent

未登录时显示“登录后使用 AI”；登录后显示 AI token 余额和“充值”入口，直接进入余额页。余额不足时说明原因并提供充值入口，完成后可以回来继续任务，不跳转到 Pro 订阅页。

## 6. 用三个例子说清楚

| 用户需求 | 实际付费与结果 |
| --- | --- |
| 我只要云托管，不用你们的 AI | 一个席位每月 $15；无需充值 AI |
| 我只在本地用内置 Agent | 无需 Pro；例如充值 $50，获得 $50 AI token 余额，按实际模型消费扣除 |
| 我既要托管，也要 AI | 一个席位每月 $15；另外自主充值 AI，两个账目分别展示 |

AI 充值不是每月固定加收的一笔费用。比如用户本月支付 $15 订阅费并充值 $50，本月共付款 $65（税费另计）；若 AI 只消费 $12，则 AI 余额还剩 $38，可继续使用，下个月不会自动再充值 $50。

## 7. 系统如何支撑，以及还有什么没落地

Desktop 负责登录入口、余额展示和本地 Agent；Cloud 负责身份验证与模型请求转发；PuppyPay 负责订阅、充值到账、余额和扣费账本；Polar 处理收款。**本地执行 Agent 与通过云端接口调用模型是两件事：模型请求经过 Cloud，不代表 Agent 在云上运行。**

付款返回页面不能直接给用户加余额，必须等待服务端验证支付结果。订阅记录按组织保存，AI 余额按个人保存；充值与扣款都要有可核对的流水，重复支付回调不能重复入账。

截至本次产品决策：

| 项目 | 实施状态 |
| --- | --- |
| 独立组织订阅与个人 AI 余额 | 已有数据模型和基础链路；测试已覆盖两者独立，不代表真实付款闭环已经验收 |
| Pro $15 / 人 / 月 | 待迁移商品、目录、权益与页面；旧目录仍保留 Plus $18、Business $20 等历史配置 |
| AI 充值金额 | 当前沙盒只有 $5 商品；更多档位待配置，任意金额输入尚未实现 |
| AI 真实毛利率 30% | 产品规则已确定；当前沙盒仍按 1 倍模型供应商成本扣费，新的成本核算与售价尚未配置 |
| 独立 AI 余额页与消费明细 | Agent 已有登录、余额和充值入口；独立页面及消费列表接口待补齐 |
| 真实沙盒验收 | 邮箱登录 → 付款 → 回调到账 → 本地 Agent 使用 → 扣费的整体验收仍待完成 |

本次只确定和记录收费规则，不代表新售价已经上线。实施核对来源：`puppyone-desktop@634603f4`、`puppyone-cloud@20e11fa2`、`PuppyPay@bc6c8eb8`；后续上线应补充新的版本与验收记录。

技术实现与沙盒操作继续维护在文档库的 [个人 Agent 额度架构](https://github.com/puppyone-ai/puppy-issues/blob/main/document/puppypay/architecture/personal-agent-credits.md)及[沙盒测试手册](https://github.com/puppyone-ai/puppy-issues/blob/main/document/puppypay/operations/desktop-agent-sandbox.md)，收费产品决策统一维护在本文。
