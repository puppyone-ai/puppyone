# 连接 Agent

让你的 AI Agent 使用 PuppyOne 中的数据。

---

## 为什么需要连接 Agent

你的数据已经在 PuppyOne 中结构化存储，现在让 Agent 用起来：

- **Cursor** 中的 AI 助手可以查询你的产品文档
- **Claude Desktop** 可以访问你的知识库
- **自研 Agent** 可以通过 API 获取实时数据

---

## 支持的连接方式

| 方式 | 适用场景 | 复杂度 |
|------|----------|--------|
| [**MCP (Cursor)**](/zh/agents/cursor) | Cursor IDE 用户 | ⭐ 最简单 |
| [**MCP (Claude Desktop)**](/zh/agents/claude-desktop) | Claude Desktop 用户 | ⭐ 最简单 |
| [**MCP (自定义)**](/zh/agents/custom-mcp) | 其他 MCP 兼容客户端 | ⭐⭐ |
| [**REST API**](/zh/agents/rest-api) | Python/Node 自研 Agent | ⭐⭐⭐ |

---

## 什么是 MCP

**MCP (Model Context Protocol)** 是 Anthropic 提出的开放协议，让 AI 模型可以访问外部工具和数据。

支持 MCP 的客户端：
- Cursor
- Claude Desktop
- Cline
- 更多...

PuppyOne 为每个 Table 提供 MCP Server，Agent 可以通过 MCP 协议：
- 查询数据
- 创建/修改数据（如果有权限）
- 获取数据结构信息

---

## 30 秒快速接入

以 Cursor 为例：

### 1. 获取 MCP 配置

1. 打开你的 Table
2. 点击 **Connect Agent** 按钮
3. 复制生成的配置

### 2. 配置 Cursor

1. 打开 Cursor Settings
2. 找到 **MCP** 设置
3. 粘贴配置：

```json
{
  "mcpServers": {
    "puppyone": {
      "command": "npx",
      "args": ["-y", "mcp-remote", "https://api.puppyone.ai/api/v1/mcp/server/YOUR_API_KEY"]
    }
  }
}
```

### 3. 重启 Cursor

重启后，Cursor 的 AI 助手就可以访问你的数据了。

### 4. 测试

在 Cursor 中问：

> "查询一下我的产品目录"

Agent 会自动调用 PuppyOne 的 Tool 获取数据。

---

## 连接流程图

```
┌─────────────┐     MCP 协议      ┌─────────────┐
│   Cursor    │ ◄───────────────► │  PuppyOne   │
│   Agent     │                   │ MCP Server  │
└─────────────┘                   └──────┬──────┘
                                         │
                                         ▼
                                  ┌─────────────┐
                                  │   Table     │
                                  │   Data      │
                                  └─────────────┘
```

---

## Agent 能做什么

连接成功后，Agent 可以使用这些 Tool：

| Tool | 功能 |
|------|------|
| `query_data` | 带条件查询数据 |
| `get_all_data` | 获取全部数据 |
| `create` | 创建新条目 |
| `update` | 修改数据 |
| `delete` | 删除数据 |

> 💡 Agent 实际能用的 Tool 取决于你的[访问控制配置](/zh/access)。

---

## 下一步

选择你的 Agent 客户端，查看详细配置指南：

import { Cards, Card } from 'nextra/components'

<Cards>
  <Card title="Cursor" href="/zh/agents/cursor" />
  <Card title="Claude Desktop" href="/zh/agents/claude-desktop" />
  <Card title="REST API" href="/zh/agents/rest-api" />
</Cards>




