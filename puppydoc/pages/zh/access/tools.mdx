# Tool 权限

Tool 是 Agent 可以调用的数据操作，通过配置 Tool 权限控制 Agent 的能力。

---

## 什么是 Tool

Tool 是 PuppyOne 暴露给 Agent 的操作接口。每个 Tool 对应一种数据操作：

| Tool | 操作类型 | 说明 |
|------|----------|------|
| `query_data` | 读取 | 带过滤条件查询数据 |
| `get_all_data` | 读取 | 获取指定路径的全部数据 |
| `create` | 写入 | 在指定路径添加新条目 |
| `update` | 写入 | 修改指定路径的现有数据 |
| `delete` | 写入 | 删除指定路径的数据 |

---

## Tool 详解

### query_data

**用途**：Agent 查询满足条件的数据

**示例调用**：
```
查询价格低于 100 的产品
→ query_data(path="/products", filter={"price": {"$lt": 100}})
```

**返回**：匹配的数据数组

**风险**：🟢 低（只读操作）

---

### get_all_data

**用途**：Agent 获取某个路径下的全部数据

**示例调用**：
```
获取所有分类
→ get_all_data(path="/categories")
```

**返回**：指定路径的完整数据

**风险**：🟢 低（只读，但注意数据量）

> ⚠️ 如果数据量很大，可能影响 Agent 的上下文长度。考虑用 `query_data` 替代。

---

### create

**用途**：Agent 添加新数据

**示例调用**：
```
添加新产品
→ create(path="/products", data={"name": "New Widget", "price": 59.99})
```

**结果**：在 `/products` 数组末尾添加新条目

**风险**：🟡 中（会新增数据）

---

### update

**用途**：Agent 修改现有数据

**示例调用**：
```
更新产品价格
→ update(path="/products/0/price", value=79.99)
```

**结果**：修改指定路径的值

**风险**：🟡 中（会修改数据）

---

### delete

**用途**：Agent 删除数据

**示例调用**：
```
删除第一个产品
→ delete(path="/products/0")
```

**结果**：删除指定路径的数据

**风险**：🔴 高（数据删除不可逆）

---

## 配置 Tool 权限

### 在 Dashboard 中配置

1. 打开 Table → **Access** 标签
2. 在 **Tools** 区域勾选要启用的 Tool
3. 点击 **Save**

### 通过 API 配置

```bash
# 创建只读 Agent
curl -X POST https://api.puppyone.ai/api/v1/agents \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "name": "Support Agent",
    "tools": ["query_data", "get_all_data"]
  }'
```

---

## 最佳实践

### 最小权限原则

只授予 Agent 完成任务所需的最小权限：

```yaml
# ❌ 不推荐：给所有权限
tools: [query_data, get_all_data, create, update, delete]

# ✅ 推荐：只给需要的权限
tools: [query_data]  # 客服 Agent 只需要查询
```

### 分级授权

| Agent 类型 | 推荐 Tools |
|-----------|-----------|
| 查询类（客服、搜索） | `query_data`, `get_all_data` |
| 编辑类（内容管理） | + `create`, `update` |
| 管理类（数据维护） | 全部，但限制路径 |

### 禁用 delete

除非确实需要，否则不要授权 `delete`：

```yaml
# 即使是管理 Agent，也可以不给 delete
tools: [query_data, get_all_data, create, update]
```

---

## 下一步

- [路径权限详解](/zh/access/paths) - 控制 Tool 能访问哪些数据
- [API Key 管理](/zh/access/api-keys) - 管理 Agent 的访问凭证



