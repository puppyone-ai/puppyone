# 操作日志查询

查看、筛选和分析 Content Node 的完整操作记录。

---

## 查看审计日志

PuppyOne 提供两种方式查看审计日志：Dashboard 和 API。

### Dashboard

1. 打开 Project，进入 **Data** 页面
2. 选中目标文件或文件夹
3. 点击右侧面板的 **Audit** 标签页
4. 查看该节点的所有操作记录

### API

调用 REST API 获取指定节点的审计日志：

```bash
curl -X GET "https://api.puppyone.ai/api/v1/nodes/{node_id}/audit-logs" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

响应示例：

```json
{
  "logs": [
    {
      "actor": "alice@team.com",
      "action": "update",
      "node_path": "/docs/readme.md",
      "timestamp": "2025-03-09T14:30:00Z",
      "metadata": {
        "source": "dashboard",
        "version": 5
      }
    },
    {
      "actor": "Research Agent",
      "action": "update",
      "node_path": "/docs/readme.md",
      "timestamp": "2025-03-09T12:15:00Z",
      "metadata": {
        "source": "mcp",
        "version": 4
      }
    }
  ]
}
```

---

## 日志条目结构

每条审计日志包含以下字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `actor` | string | 操作者身份——用户邮箱（如 `alice@team.com`）或 Agent 名称（如 `Research Agent`） |
| `action` | string | 操作类型，见下方列表 |
| `node_path` | string | 被操作的 Content Node 路径 |
| `timestamp` | string | 操作时间（ISO 8601 格式） |
| `metadata` | object | 附加信息，包含操作来源、版本号等 |

---

## 操作类型

| 操作类型 | 说明 | 典型场景 |
|----------|------|----------|
| `create` | 创建新节点 | 新建文件、文件夹；Agent 生成新内容 |
| `update` | 修改节点内容 | 编辑文件内容；Agent 更新数据 |
| `delete` | 删除节点 | 删除文件或文件夹 |
| `rollback` | 回滚到历史版本 | 恢复到之前的版本 |
| `sync` | 从外部数据源同步 | Notion / GitHub 等连接器同步数据 |
| `checkout` | 检出节点 | 锁定节点进行编辑 |
| `commit` | 提交检出的修改 | 完成编辑，释放锁定 |

---

## 筛选和搜索

通过 API 获取审计日志后，你可以在自己的程序中按字段筛选。也可以将结果导出后配合 `jq` 做本地过滤：

```bash
# 获取审计日志并用 jq 筛选特定操作者
curl -X GET "https://api.puppyone.ai/api/v1/nodes/{node_id}/audit-logs" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  | jq '.logs[] | select(.actor == "Research Agent")'
```

---

## 实际场景

### 场景一：谁最后修改了这个文件？

```bash
curl -X GET "https://api.puppyone.ai/api/v1/nodes/{node_id}/audit-logs" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

日志最上方就是最近一次操作，可以直接看到修改者和时间。

### 场景二：某个 Agent 在过去 24 小时做了什么？

```bash
# 获取审计日志后，用 jq 筛选特定时间范围和 Agent
curl -X GET "https://api.puppyone.ai/api/v1/nodes/{node_id}/audit-logs" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  | jq '[.logs[] | select(.actor == "Content Writer")]'
```

### 场景三：排查数据异常

当发现数据不正确时，通过审计日志定位问题，再结合版本历史恢复：

```bash
# 1. 查看该文件的完整操作历史
curl -X GET "https://api.puppyone.ai/api/v1/nodes/{node_id}/audit-logs" \
  -H "Authorization: Bearer YOUR_TOKEN"

# 2. 结合版本历史，对比变更前后的内容差异
curl -X GET "https://api.puppyone.ai/api/v1/content/{project_id}/versions?path=/data/pricing.json" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

找到导致问题的那次修改后，可以使用版本回滚恢复数据。

---

## 下一步

- [版本控制](/zh/version-control) — 查看和回滚文件版本
- [冲突解决](/zh/conflict) — 处理多方并发编辑
- [Auth for Agents](/zh/auth-for-agents) — 控制 Agent 的访问范围
