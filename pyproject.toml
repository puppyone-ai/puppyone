[project]
name = "contextbase"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "chromadb>=1.3.4",
    "fastapi>=0.121.1",
    "fastmcp>=2.13.0.2",
    "jmespath>=1.0.1",
    "jsonpointer>=3.0.0",
    "openai>=2.8.0",
    "psutil>=7.1.3",
    "pydantic>=2.12.4",
    "pyjwt>=2.10.1",
    "python-multipart>=0.0.20",
    "sentence-transformers>=5.1.2",
    "transformers>=4.57.1",
    "uvicorn>=0.38.0",
    # 文件解析依赖（本地解析模式）
    "pdfplumber>=0.11.0",  # PDF 解析（优先使用，支持表格）
    "PyPDF2>=3.0.0",  # PDF 解析（回退方案）
    "python-docx>=1.1.0",  # DOCX 解析
    "python-pptx>=0.6.23",  # PPTX 解析
    "Pillow>=10.0.0",  # 图片处理
    "requests>=2.31.0",  # HTTP 请求（MinerU API）
]
